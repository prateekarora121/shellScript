name: PR

on: workflow_dispatch
defaults:
  run:
    shell: bash -leo pipefail {0}
jobs:
  Main:
    uses: ./.github/workflows/workflow_a.yml
    secrets: inherit
  TestArtifacts:
    runs-on: ubuntu-latest
    needs: [Main]
    steps:
      - name: actions/download-artifacts@v3
        with: 
          name: workflow-${{ github.sha}}
      - name: Check files
        run: |
          ls -a
          cat test.txt
